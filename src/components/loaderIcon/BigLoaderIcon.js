import React from 'react';

import { theme } from '@@config';
import LoaderIcon from './LoaderIcon.container';

const BigLoaderIcon = () => <LoaderIcon size={40} color={theme.colors.black} />;

export default BigLoaderIcon;
